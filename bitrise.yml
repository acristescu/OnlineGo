format_version: '25'
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git
project_type: android
workflows:
  deploy:
    triggers:
      push:
        - branch: "release"
    description: |
      ## How to get a signed APK
      Deploy to play store

    steps:
      - activate-ssh-key@4.1:
          run_if: '{{getenv "SSH_RSA_PRIVATE_KEY" | ne ""}}'
      - set-java-version@1:
          inputs:
            - set_java_version: '17'
      - script@1:
          inputs:
            - content: sdkmanager "cmake;3.31.6"
      - git-clone@6.2: {}
      - cache-pull@2.7: {}
      - file-downloader@1.1:
          inputs:
            - destination: "$BITRISE_SOURCE_DIR/app/google-services.json"
            - source: "$BITRISEIO_GOOGLE_SERVICES_JSON_URL"
      - git::https://github.com/viktorbenei/bitrise-step-install-android-ndk-version.git@master:
          inputs:
            - android_ndk_version: r19b
      - change-android-versioncode-and-versionname@1.3:
          inputs:
            - version_code_offset: '53'
            - build_gradle_path: "$PROJECT_LOCATION/$MODULE/build.gradle.kts"
      - android-unit-test@1.0:
          inputs:
            - project_location: "$PROJECT_LOCATION"
            - module: "$MODULE"
            - variant: "$TEST_VARIANT"
      - android-build@1.0:
          inputs:
            - project_location: "$PROJECT_LOCATION"
            - module: "$MODULE"
            - variant: release
            - build_type: apk
      - android-build@1.0:
          inputs:
            - project_location: "$PROJECT_LOCATION"
            - module: "$MODULE"
            - variant: release
            - build_type: aab
      - sign-apk@1.7:
          run_if: '{{getenv "BITRISEIO_ANDROID_KEYSTORE_URL" | ne ""}}'
          inputs:
            - android_app: |-
                $BITRISE_APK_PATH
                $BITRISE_AAB_PATH
            - use_apk_signer: 'true'
            - signer_scheme: v2
      - github-release@0:
          inputs:
            - username: "$GITHUB_USERNAME"
            - name: "$ANDROID_VERSION_NAME$ANDROID_VERSION_CODE"
            - body: "$ANDROID_VERSION_NAME$ANDROID_VERSION_CODE"
            - draft: 'no'
            - tag: "$ANDROID_VERSION_NAME$ANDROID_VERSION_CODE"
            - commit: release
            - files_to_upload: "$BITRISE_SIGNED_APK_PATH"
            - api_token: "$GITHUB_TOKEN"
      - google-play-deploy@3.8:
          inputs:
            - package_name: io.zenandroid.onlinego
            - track: production
            - app_path: "$BITRISE_SIGNED_AAB_PATH"
            - service_account_json_key_path: "$BITRISEIO_SERVICE_ACCOUNT_JSON_KEY_URL_URL"
      - deploy-to-bitrise-io@2.0: {}
      - cache-push@2.7: {}
  primary:
    triggers:
      push:
        - branch: "master"
      pull_request:
        - source_branch: "/^(?!.*l10n_master).*/"
    steps:
      - activate-ssh-key@4.1:
          run_if: '{{getenv "SSH_RSA_PRIVATE_KEY" | ne ""}}'
      - set-java-version@1.2.1:
          inputs:
            - set_java_version: '17'
      - script@1:
          inputs:
            - content: sdkmanager "cmake;3.10.2.4988404"
      - git-clone@6.2: {}
      - cache-pull@2.7: {}
      - file-downloader@1.1:
          inputs:
            - destination: "$BITRISE_SOURCE_DIR/app/google-services.json"
            - source: "$BITRISEIO_GOOGLE_SERVICES_JSON_URL"
      - git::https://github.com/viktorbenei/bitrise-step-install-android-ndk-version.git@master:
          inputs:
            - android_ndk_version: r19b
      - android-unit-test@1.0:
          inputs:
            - project_location: "$PROJECT_LOCATION"
            - module: "$MODULE"
            - variant: "$TEST_VARIANT"
      - deploy-to-bitrise-io@2.0: {}
      - cache-push@2.7: {}
app:
  envs:
    - opts:
        is_expand: false
      PROJECT_LOCATION: "."
    - opts:
        is_expand: false
      GRADLEW_PATH: gradlew
    - opts:
        is_expand: false
      MODULE: app
    - opts:
        is_expand: false
      BUILD_VARIANT: Debug
    - opts:
        is_expand: false
      TEST_VARIANT: Debug
meta:
  bitrise.io:
    stack: linux-docker-android-22.04
