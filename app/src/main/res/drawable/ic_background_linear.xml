<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/accentGradientStart">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@color/accentGradientEnd" />
            <corners android:radius="14dp" />
        </shape>
    </item>

    <item android:id="@android:id/background">
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:endColor="@color/accentGradientEnd"
                android:startColor="@color/accentGradientStart"
                android:type="linear" />
            <corners android:radius="14dp" />
        </shape>
    </item>
</ripple>